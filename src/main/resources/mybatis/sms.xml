<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="dev.mvc.sms.SmsDAOInter">
  <!--  로그인 내역 목록  -->
  <select id="list_all" parameterType="dev.mvc.sms.SmsVO">
    SELECT loginno, customerno, ip, logindate FROM login ORDER BY loginno DESC
  </select>
  
  <!-- 로그인 내역 조회 -->
  <select id="read" resultType="dev.mvc.sms.SmsVO" parameterType="int">
    SELECT loginno, customerno, ip, logindate
    FROM login
    WHERE loginno =#{loginno}
  </select>
  
   <!-- 로그인 내역 삭제 -->
   <delete id="delete" parameterType="int">
    DELETE FROM login
    WHERE loginno=#{loginno}
   </delete>
   </mapper>

